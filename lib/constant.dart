/// Size used for feedback block
const int BLOCK_FEEDBACK_SIZE = 70;

/// Size used for queued draggable blocks
const num BLOCK_USE_SIZE = 25;

/// number of blocks available
const int BLOCK_USEABLE_COUNT = 3;

enum GameBlockSkin {
  aa,
}
enum GameModes {
  Normal,
}
